# Notion internal integration token (secret).
# 1. Create an internal integration: https://www.notion.so/my-integrations
# 2. Share BOTH pages with it: "List of employees" and "Salary ranges".
# 3. Copy the token here, then run `npm run ingest`.
NOTION_TOKEN=ntn_your_token_here
