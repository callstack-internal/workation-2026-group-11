// Base URL of the CallCost backend API.
//
// When deploying the server elsewhere, update this value AND the matching
// entry in `host_permissions` in manifest.config.ts.
export const SERVER_URL = "http://localhost:3000";
